#include "main.h"

/**
 * @a first integar
 * @b second integar
 * @c third imtegar
 * Return: largest number
 */

int largest_number(int a, int b , int c)
{
	int largest;

	if (a > 0)
	{
		if (b > c)
			largest = a;
		else if (a > c_)
			largest = a;
		else
			largest = c;
	}
	else
		if (b <c)
			largest = c;
		else
			largest = b;
}
	
