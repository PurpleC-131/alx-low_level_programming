#include <stdio.h>

/**
 * main - prints "Programming is like building a multilingual puzzle
 * @void:Function argument
 * Return:0 if the code runs successfully 1 otherwise
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
